import React from 'react';
import {View, Text} from 'react-native';

const SendMessage = () => {
    return (
        <View>
            <Text>
                SendMessage
            </Text>
        </View>
    );
};

export default SendMessage;